name: Expensive integration fixture
on:
  pull_request:
jobs:
  verify:
    if: ${{ false }}
    runs-on: macos-latest
    timeout-minutes: 10
    steps:
      - run: echo "MinuteShield integration fixture"
